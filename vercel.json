{
  "buildCommand": "npm --prefix petchat install && npm --prefix petchat run build",
  "outputDirectory": "petchat/dist",
  "cleanUrls": true,
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
